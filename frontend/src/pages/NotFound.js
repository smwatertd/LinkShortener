import { Typography } from "@mui/material";

const NotFound = () => {
  return (
    <Typography
      sx={{
        position: "absolute",
        bottom: "50%",
        left: "50%",
      }}
    >
      Not Found
    </Typography>
  );
};

export { NotFound };
